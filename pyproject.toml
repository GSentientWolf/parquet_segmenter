[project]
name = "parquet-segmenter"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
	
]

[project.scripts]
parquet-seg-cli = "parquet_segmenter.cli:main"
assert-no-src-testing = "parquet_segmenter.scripts:assert_no_src_testing_main"

